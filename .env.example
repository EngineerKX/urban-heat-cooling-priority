# copy this file to .env and fill in your own credentials — never commit .env itself
GEE_SERVICE_ACCOUNT=
GEE_PRIVATE_KEY_PATH=
GEE_PROJECT_ID=nus-iss-urban-heat-sg
# GCS bucket for U-Net TFRecord patch export/import only (see src/ingest/gee.py
# export_patches_to_gcs). Leave blank if you're not training U-Net yet.
GEE_EXPORT_BUCKET=